# ─── Local development ───
# Leave blank — Vite proxy (/ck-api → localhost:5000/api) is used automatically.
VITE_API_BASE_URL=

VITE_APP_DOWNLOAD_URL=https://play.google.com/store

# ─── Production (Vercel) ───
# Live API URL is in `.env.production` → https://callkaro.delicod.com/api
# (used only on build / Vercel).
