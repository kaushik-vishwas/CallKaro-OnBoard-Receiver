# Loaded automatically by `vite build` / Vercel production.
# Local `npm run dev` ignores this and uses the Vite proxy instead.
VITE_API_BASE_URL=https://callkaro.delicod.com/api
VITE_APP_DOWNLOAD_URL=https://play.google.com/store
